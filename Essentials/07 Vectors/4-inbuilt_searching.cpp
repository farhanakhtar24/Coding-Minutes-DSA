/*
Inbuilt function for doing linear search is find function
*/

#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main()
{
    vector<int> arr = {10, 10, 2, 11, 31, 2, 3, 4, 5, 6, 7};

    vector<int>::iterator it = find(arr.begin(), arr.end(), );

    return 0;
}