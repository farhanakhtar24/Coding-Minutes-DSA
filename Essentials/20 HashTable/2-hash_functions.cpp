/*
Good Hash Function :-
1.Reduces chances of collison :- Distribute keys uniformaly over the table
2.Should be fast to compute
*/

#include <iostream>
#include "./HashTable.h"
using namespace std;

int main()
{
    HashTable<int> h;
    h.;
    // h.insert("mango", 100);
    // h.insert("mango", 100);
    // h.insert("mango", 100);
    // h.insert("mango", 100);
    // h.insert("mango", 100);
    return 0;
}
