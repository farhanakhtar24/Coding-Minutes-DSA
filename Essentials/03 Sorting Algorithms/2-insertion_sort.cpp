#include <iostream>
using namespace std;

void insertionSortAlgo(int arr[], int n)
{

    for (int i = 1; i < n; i++)
    {
        for (int j = i; j < n; j++)
        {
                }
    }
}

int main()
{
    int arr[] = {-2, 3, -12, 4, -1, 5, 6, 1, 3};
    int n = sizeof(arr) / sizeof(int);
    return 0;
}