#include <iostream>
#include <stack>

using namespace std;

void stockSpan(int prices[], int n, int span[])
{
    stack<int> s; // indicesof days
    s.push(0);
    span[0] = 1;

    for (int i = 0; i <= n - 1; i++)
    {
        }
}

int main()
{

    return 0;
}