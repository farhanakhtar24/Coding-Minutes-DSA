/*
Heap:
1.It is a binary tree
2.It is a complete binary tree(a tree in which all levels are completely filled except the last level,
  but the level should be filled in left to right order)
3.Should follow heap order property
  For Max heap : parent >= children nodes
  For Min heap : parent <= children nodes
*/